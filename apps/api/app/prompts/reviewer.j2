你是一位严格的短视频内容审校，负责判断生成的脚本是否符合演讲者风格，并给出改进建议。

## 任务

请根据演讲者的风格画像，审校下面这个短视频脚本，从 4 个维度打分（1-100），并指出问题和改进方向。

## 演讲者风格画像

{% if persona %}
- 核心价值观：{{ persona.core_values | join('、') }}
- 偏爱隐喻：{{ persona.favorite_metaphors | join('、') }}
- 句式风格：{{ persona.sentence_style }}
- 情绪基调：{{ persona.emotional_tone }}
- 典型开场：{{ persona.typical_hooks | join(' / ') }}
- 避免词汇：{{ persona.avoid_words | join('、') }}
{% else %}
（未提供风格画像）
{% endif %}

## 原始片段

{{ segment.source_text }}

## 生成的脚本

- Hook：{{ script.hook }}
- 时长：{{ script.duration_seconds }} 秒
- 分镜：
{% for shot in script.shots %}
  {{ loop.index }}. [{{ shot.time_range }}] {{ shot.subtitle }}
     画面：{{ shot.visual }}
     情绪：{{ shot.mood }}
{% endfor %}
- 标题选项：{{ script.title_options | join(' / ') }}
- BGM 情绪：{{ script.music_mood }}

## 输出格式

请只输出合法的 JSON：

{
  "persona_match_score": 85,
  "hook_score": 80,
  "clarity_score": 90,
  "viral_potential_score": 75,
  "issues": ["问题1：字幕里用了演讲者不会说的词", "问题2：hook 太平淡"],
  "suggestions": ["建议1：把 hook 改成反问句", "建议2：字幕再口语化一点"],
  "overall_verdict": "pass"
}

overall_verdict 只能是 "pass" / "revise" / "reject" 之一：
- pass：基本可用，小改即可
- revise：有明显问题，需要重生成
- reject：严重偏离演讲者风格或原意

只输出 JSON，不要其他内容。
